[project]
name = "bbot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fast-agent-mcp",
    "mcp-datetime",
    "mcp-server-calculator",
    "mcp-server-fetch",
    "mcp-weather-server",
]